import { colors } from './colors';
import { sizes } from './sizes';
import { fonts, fontWeights, fontSizes } from './fonts';

export const theme = {
    colors,
    sizes,
    fonts,
    fontWeights,
    fontSizes,
};
